kind = "web"
previewPath = "/"
title = "CubeAI Predictive Maintenance"
version = "1.0.0"
id = "artifacts/cubeai-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22164

[services.development]
run = "pnpm --filter @workspace/cubeai-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cubeai-dashboard", "run", "build" ]
publicDir = "artifacts/cubeai-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22164"
BASE_PATH = "/"
